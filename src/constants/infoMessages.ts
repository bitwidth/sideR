export const InfoMessages = {
  PERSISTING_DATA_TO_DISK_STORAGE: (path: string) => `Persisting data to disk location: ${path}`,
  PERSISTING_METADATA_TO_DISK_STORAGE: (path: string) => `Persisting metadata to disk location: ${path}`,
  RESTORING_DATA_FROM_DISK_STORAGE: (path: string) => `Restoring data state from disk location: ${path}`,
  RESTORING_METADATA_FROM_DISK_STORAGE: (path: string) => `Restoring metadata state from disk location: ${path}`,
}